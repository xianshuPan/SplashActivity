<?xml version="1.0" encoding="utf-8"?>
 
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical">
    
    <ProgressBar 
        android:id="@+id/refresh_list_footer_progressbar" 
        android:layout_width="20dp"
        android:layout_height="20dp" 
        > 
    </ProgressBar> 
   	<TextView android:id="@+id/refresh_list_footer_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textSize="15sp"
            android:textColor="@color/color_hint_txt"
            android:text="@string/list_footer_load_more"/>
    <View
        android:visibility="gone"
        android:id="@+id/refresh_list_footer_bottom_view"
        android:layout_marginTop="55dp"
        android:layout_width="match_parent"
        android:layout_height="1px"/>
</LinearLayout>